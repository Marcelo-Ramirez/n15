"use client";
import { Box } from "@chakra-ui/react";

export default function DashboardPage() {
  // El contenido se renderiza en MainContent basado en el activeMenuItem
  // Esta página ahora solo sirve como placeholder
  return (
    <Box>
      {/* El contenido se maneja en el layout con MainContent */}
    </Box>
  );
}
